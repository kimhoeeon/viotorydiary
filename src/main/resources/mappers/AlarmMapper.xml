<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.viotory.diary.mapper.AlarmMapper">

    <select id="selectAlarmList" resultType="com.viotory.diary.vo.AlarmVO">
        SELECT *
        FROM alarms
        WHERE member_id = #{memberId}
          AND created_at <![CDATA[>]]> DATE_SUB(NOW(), INTERVAL 7 DAY)
        ORDER BY created_at DESC
    </select>

    <insert id="insertAlarm" parameterType="com.viotory.diary.vo.AlarmVO">
        INSERT INTO alarms (member_id, category, title, content, redirect_url, is_read, created_at)
        VALUES (#{memberId}, #{category}, #{title}, #{content}, #{redirectUrl}, 0, NOW())
    </insert>

    <update id="updateReadStatus">
        UPDATE alarms
        SET is_read = 1
        WHERE alarm_id = #{alarmId}
          AND member_id = #{memberId}
    </update>

    <select id="countUnread" resultType="int">
        SELECT COUNT(*)
        FROM alarms
        WHERE member_id = #{memberId}
          AND is_read = 0
          AND created_at <![CDATA[>]]> DATE_SUB(NOW(), INTERVAL 7 DAY)
    </select>

    <select id="selectUnreadCount" parameterType="long" resultType="int">
        SELECT COUNT(*)
        FROM alarms
        WHERE member_id = #{memberId}
          AND is_read = 0
    </select>

    <delete id="deleteOldAlarms">
        DELETE FROM alarms
        WHERE created_at <![CDATA[<]]> DATE_SUB(NOW(), INTERVAL 7 DAY)
    </delete>

</mapper>