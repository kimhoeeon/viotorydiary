<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.viotory.diary.mapper.PushMngMapper">

    <select id="selectPushLogList" resultType="com.viotory.diary.vo.PushLogVO">
        SELECT * FROM push_logs
        ORDER BY created_at DESC
    </select>

    <insert id="insertPushLog" parameterType="com.viotory.diary.vo.PushLogVO">
        INSERT INTO push_logs (title, content, link_url, target_user, send_count, status, created_at)
        VALUES (#{title}, #{content}, #{linkUrl}, #{targetUser}, #{sendCount}, #{status}, NOW())
    </insert>

    <select id="countAllMembers" resultType="int">
        SELECT COUNT(*) FROM members WHERE status = 'ACTIVE'
    </select>

    <select id="selectTargetFcmTokens" resultType="string">
        SELECT fcm_token
        FROM members
        WHERE fcm_token IS NOT NULL
        AND fcm_token != ''
        AND status = 'ACTIVE'
        <if test="targetType == 'TEAM' and targetTeam != null and targetTeam != ''">
            AND my_team_code = #{targetTeam}
        </if>
        <if test="targetType == 'INACTIVE'">
            AND (last_login_at <![CDATA[ <= ]]> DATE_SUB(NOW(), INTERVAL 7 DAY) OR last_login_at IS NULL)
        </if>
    </select>

    <select id="selectTargetMemberIds" resultType="long">
        SELECT member_id
        FROM members
        WHERE status = 'ACTIVE'
        <if test="targetType == 'TEAM' and targetTeam != null and targetTeam != ''">
            AND my_team_code = #{targetTeam}
        </if>
        <if test="targetType == 'INACTIVE'">
            AND (last_login_at <![CDATA[ <= ]]> DATE_SUB(NOW(), INTERVAL 7 DAY) OR last_login_at IS NULL)
        </if>
    </select>

</mapper>