<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.viotory.diary.mapper.AdminMapper">

    <select id="selectAdminByLoginId" resultType="com.viotory.diary.vo.AdminVO">
        SELECT
            admin_id,
            login_id,
            password,
            name,
            role,
            last_login_at,
            created_at
        FROM admins
        WHERE login_id = #{loginId}
    </select>

    <select id="selectAllowedIpsByAdminId" resultType="string">
        SELECT ip_address
        FROM admin_access_ips
        WHERE admin_id = #{adminId}
    </select>

    <select id="selectAdminEmailsByAdminId" resultType="com.viotory.diary.vo.AdminEmailVO">
        SELECT *
        FROM admin_emails
        WHERE admin_id = #{adminId}
    </select>

    <update id="updateLastLogin" parameterType="long">
        UPDATE admins SET last_login_at = NOW() WHERE admin_id = #{adminId}
    </update>

    <update id="updatePassword">
        UPDATE admins SET password = #{password} WHERE login_id = #{loginId}
    </update>

</mapper>