<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.viotory.diary.mapper.AdminMapper">

    <select id="selectAdminByLoginId" parameterType="string" resultType="com.viotory.diary.vo.AdminVO">
        SELECT
            admin_id AS adminId,
            login_id AS loginId,
            password,
            name,
            role,
            allowed_ip AS allowedIp,
            last_login_at AS lastLoginAt,
            created_at AS createdAt
        FROM admins
        WHERE login_id = #{loginId}
    </select>

    <update id="updateLastLogin" parameterType="long">
        UPDATE admins
        SET last_login_at = NOW()
        WHERE admin_id = #{adminId}
    </update>

</mapper>